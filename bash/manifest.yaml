schemaVersion: 1
id: bash
displayName: Bash
rank: 9
selectionTier: ranked
implementation:
  status: attempting
  provenance: native
  version: 0.1.0
  notes: Native Bash implementation of Convex's documented JSON HTTP API. It uses BusyBox wget as an ordinary HTTP/TLS transport and jq as the runtime JSON parser; it does not invoke another Convex client or runtime.
toolchain:
  name: GNU Bash
  version: 5.2.37
syncProfile:
  id: convex-rs-0.10.4-unversioned-sync
  sourceCommit: 6f1df8a8ba1665084ec001e307ca841ca17074d7
  endpoint: /api/sync
intendedCapabilities: [http, live]
platforms: [linux/amd64]
adapter:
  protocolVersion: 1
  entrypoint: /usr/local/bin/convex-adapter
  adapterOnlyCommands: [debugDisconnect]
limitations:
  - Live uses a direct Bash RFC6455 implementation. It supports the pinned sync profile only, with an intentionally bounded newest-16 delivery policy in the adapter.
  - The runtime deliberately includes jq for correct UTF-8 and nested JSON handling and BusyBox wget for HTTP/TLS.
capabilities: []
