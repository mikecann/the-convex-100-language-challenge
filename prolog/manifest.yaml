schemaVersion: 1
id: prolog
displayName: "Prolog"
rank: 30
selectionTier: ranked
implementation:
  status: working
  provenance: native
  version: 0.2.0
  notes: Native SWI-Prolog HTTP and WebSocket implementation. Shared local and hosted conformance earned HTTP and Live.
toolchain:
  name: SWI-Prolog
  version: 9.2.8
syncProfile:
  id: convex-rs-0.10.4-unversioned-sync
  sourceCommit: 6f1df8a8ba1665084ec001e307ca841ca17074d7
  endpoint: /api/sync
intendedCapabilities:
  - http
  - live
platforms:
  - linux/amd64
adapter:
  protocolVersion: 1
  entrypoint: /usr/local/bin/convex-adapter
  adapterOnlyCommands:
    - debugDisconnect
limitations:
  - Live authentication, WebSocket mutations/actions, transition chunks, and optimistic updates are deferred.
  - The Live owner globally retains the newest 16 updates within a 16 MiB encoded-byte budget and rejects an event over 2 MiB.
  - The adapter output queue is globally limited to 16 events and 8 MiB of encoded JSON.
capabilities:
  - http
  - live
