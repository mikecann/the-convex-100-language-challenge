schemaVersion: 1
id: julia
displayName: "Julia"
rank: 32
selectionTier: ranked
implementation:
  status: attempting
  provenance: native
  version: 0.1.0
  notes: Native Julia implementation of Convex JSON HTTP and the pinned unversioned /api/sync profile. Capability badges stay empty until final-image memory and minimality gates plus root-owned local and hosted black-box suites pass.
toolchain:
  name: Julia
  version: 1.11.6
syncProfile:
  id: convex-rs-0.10.4-unversioned-sync
  sourceCommit: 6f1df8a8ba1665084ec001e307ca841ca17074d7
  endpoint: /api/sync
intendedCapabilities: [http, live]
platforms: [linux/amd64]
adapter:
  protocolVersion: 1
  entrypoint: /usr/local/bin/convex-adapter
  adapterOnlyCommands: [debugDisconnect]
limitations:
  - Live and adapter delivery share a global newest-value budget of 5 client records, 7 output records, and 4 charged transient slots, bounded to 14 MiB of encoded payload.
  - Live authentication, WebSocket mutations and actions, optimistic updates, journals, and TransitionChunk assembly are deferred.
  - The client accepts the JSON-safe Convex value subset used by this experiment.
  - Capability badges remain unearned until root-owned shared verification passes.
capabilities: []
