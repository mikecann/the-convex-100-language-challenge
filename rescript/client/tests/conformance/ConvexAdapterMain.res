// The executable installed as /usr/local/bin/convex-adapter in the runtime
// image. It exists only so ConvexAdapter itself stays importable by tests
// without starting a transport as a side effect of being loaded.

ConvexAdapter.main()
