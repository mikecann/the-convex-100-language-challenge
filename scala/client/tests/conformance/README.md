# Scala conformance adapter

`convex.Adapter` is the test-only NDJSON protocol-v1 executable compiled into
the runtime image. This directory is reserved for its protocol fixtures; the
adapter source currently remains with the Scala client sources so Maven can
compile the single self-contained educational artifact.
